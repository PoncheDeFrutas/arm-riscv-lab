---
title: "06.7 · Lectura guiada de memoria"
subtitle: "Un archivo completo para separar secciones, direcciones, contenido y modos de acceso."
description: "Lectura guiada de un programa AArch64 con datos, loads, stores, arrays y syscalls."
---

::: {.breadcrumb-lite}
Cursos > AArch64 > Memoria > Lectura guiada
:::

::: {.hero-lab}
Esta página reúne las piezas de la unidad. El objetivo es leer un archivo `.s`
como lo leerás en prácticas futuras: primero secciones y símbolos, luego
direcciones, luego cargas, stores y recorrido de memoria.
:::

## Qué vas a aprender

Vas a leer un programa completo usando preguntas ordenadas. No basta con decir
“este código usa memoria”: debes identificar qué símbolos existen, en qué
sección están, qué registros contienen direcciones, qué instrucciones acceden a
memoria y qué tamaño se lee o escribe.

## Programa completo

Guarda este archivo como `memoria.s`.

```asm
.section .rodata
mensaje:
    .ascii "Memoria lista\n"
len = . - mensaje

.data
contador:
    .quad 0

array:
    .quad 10, 20, 30

.bss
buffer:
    .skip 16

.text
.global _start
_start:
    ldr x0, =contador
    mov x1, #3
    str x1, [x0]

    ldr x2, =array
    ldr x3, [x2], #8
    ldr x4, [x2], #8
    add x3, x3, x4
    ldr x4, [x2], #8
    add x3, x3, x4

    ldr x5, =buffer
    str x3, [x5]

    mov x0, #1
    ldr x1, =mensaje
    mov x2, #len
    mov x8, #64
    svc #0

    mov x0, #0
    mov x8, #93
    svc #0
```

## Construir y ejecutar

```bash
$ aarch64-linux-gnu-as memoria.s -o memoria.o
$ aarch64-linux-gnu-ld memoria.o -o memoria
$ qemu-aarch64 ./memoria
Memoria lista
$ echo $?
0
```

En Raspberry Pi ARM64 nativa, usa `as`, `ld` y `./memoria`.

## Primera lectura: secciones

| Sección | Símbolo | Papel |
|---|---|---|
| `.rodata` | `mensaje`, `len` | bytes constantes para `write` |
| `.data` | `contador`, `array` | datos modificables con valor inicial |
| `.bss` | `buffer` | espacio reservado |
| `.text` | `_start` | instrucciones que ejecuta el CPU |

## Segunda lectura: dirección vs contenido

```asm
ldr x0, =contador
mov x1, #3
str x1, [x0]
```

`x0` recibe la dirección de `contador`. `x1` recibe el valor `3`. `str` guarda
ese valor en la memoria apuntada por `x0`.

```bash
contador antes:   00 00 00 00 00 00 00 00
contador después: 03 00 00 00 00 00 00 00
```

## Tercera lectura: recorrido de array

```asm
ldr x2, =array
ldr x3, [x2], #8
ldr x4, [x2], #8
add x3, x3, x4
ldr x4, [x2], #8
add x3, x3, x4
```

`x2` empieza apuntando a `array[0]`. Cada `ldr ..., [x2], #8` lee un elemento y
avanza al siguiente. `x3` queda con `10 + 20 + 30`.

## Cuarta lectura: guardar resultado

```asm
ldr x5, =buffer
str x3, [x5]
```

`buffer` está en `.bss`. Reservó espacio, y aquí se escribe el resultado de la
suma en sus primeros 8 bytes.

## Quinta lectura: syscall `write`

```asm
mov x0, #1
ldr x1, =mensaje
mov x2, #len
mov x8, #64
svc #0
```

Esta parte viene de syscalls:

| Registro | Significado |
|---|---|
| `x0` | file descriptor `1`, stdout |
| `x1` | dirección del mensaje |
| `x2` | cantidad de bytes |
| `x8` | syscall `write`, número `64` |

## Mirar bytes del binario

```bash
$ objdump -s -j .data memoria
$ objdump -s -j .rodata memoria
```

`objdump` muestra bytes por sección. Usa esto para comprobar que `.data` tiene
datos iniciales y `.rodata` contiene el mensaje.

## Diagnóstico de lectura

Cuando leas un programa similar, usa esta lista:

1. clasifica secciones;
2. lista símbolos y qué dirección nombran;
3. marca registros que contienen direcciones;
4. marca instrucciones con corchetes;
5. identifica tamaños de acceso;
6. revisa si el puntero cambia;
7. separa cálculo en registros de escritura a memoria;
8. revisa syscalls al final.

## Errores comunes

| Error | Corrección |
|---|---|
| Perder de vista que `x2` cambia con post-index | anota puntero antes/después |
| Pensar que `buffer` ya contiene la suma | solo después de `str x3, [x5]` |
| Confundir `len` con dirección | `len` es valor calculado, no buffer |
| Creer que el programa imprime la suma | imprime mensaje, no el resultado |
| No distinguir `.data` de `.bss` | `.data` tiene valores iniciales; `.bss` reserva |

## Preguntas de cierre

- ¿Qué registros contienen direcciones?
- ¿Qué instrucciones usan corchetes?
- ¿Dónde se escribe el valor `3`?
- ¿Qué instrucción avanza el puntero del array?
- ¿Por qué `buffer` no necesita listar 16 bytes en el archivo?
- ¿Qué parte imprime y qué parte solo modifica memoria?
- ¿Qué valor se guarda en `buffer`?

## Checklist

- [ ] Puedo clasificar secciones.
- [ ] Puedo distinguir dirección y contenido.
- [ ] Puedo leer `str x1, [x0]`.
- [ ] Puedo leer post-index.
- [ ] Puedo seguir una suma de array.
- [ ] Puedo separar trabajo de memoria de syscall `write`.

## Cierre

Esta unidad prepara el terreno para arrays con loops, strings, stack frames y
debugging. La base no cambia: identifica dirección, tamaño, contenido y modo de
direccionamiento antes de interpretar un acceso a memoria.

## Fuentes {.unlisted .unnumbered}

- Arm, [Learn the Architecture - A-profile](https://www.arm.com/architecture/learn-the-architecture/a-profile).
- Arm, [Arm Architecture Reference Manual for A-profile architecture](https://developer.arm.com/documentation/ddi0487/latest).
- GNU Binutils, [Using as](https://sourceware.org/binutils/docs/as/).
- GNU Binutils, [objdump](https://sourceware.org/binutils/docs/binutils/objdump.html).
- Linux man-pages, [`write(2)`](https://www.man7.org/linux/man-pages/man2/write.2.html).
